import 'package:flutter/material.dart';

class AppWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(//wedget generico para conter todos os outros

      title: "Slidy_Modular",
      initialRoute: '/',
      
    );
  }
}